import ArrowBackIcon from '@mui/icons-material/ArrowBack';

export const ArrowBack: React.FC = () => {
    return (
        <ArrowBackIcon sx={{ mr: 1, color: 'primary.main', fontSize: 24 }} />
    );
};